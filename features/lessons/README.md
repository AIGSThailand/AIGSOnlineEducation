# Lessons domain

Reusable lesson content (LearnDash `sfwd-lessons`).

## Tables
- `lessons`

## Notes
- Placement in a course is defined by `course_steps`, not `lessons.course_id`.
- Deprecated columns on `lessons`: `course_id`, `module_id`, `sort_order`.
